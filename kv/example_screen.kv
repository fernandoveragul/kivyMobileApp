<ExampleRunScreen>:
    MDRelativeLayout:
        MDBoxLayout:
            orientation: 'vertical'
            padding: 10
            MDScrollView:
                size_hint: 1, 1
                do_scroll_x: False
                QuestionLabel:
                    id: question_text
                    adaptive_height: True
                    markup: True
                    font_style: 'H6'
                    halign: 'left'
                    valign: 'middle'
                    text: app.game_summary
            AnswerField:
                id: answer_field
                orientation: 'vertical'
                adaptive_height: True
            MDLabel:
                size_hint_y: 1
        MDFloatingActionButton:
            icon: 'exit-run'
            pos_hint: {'center_x': .1, 'center_y': .1}
            elevation: 0
            on_release:
                root.show_end_game_dg()
                question_text.text = app.game_summary
                app.switch_to_main_screen()
        MDFloatingActionButton:
            icon: 'help'
            pos_hint: {'center_x': .9, 'center_y': .1}
            elevation: 0
            on_release:
                root.show_tip_dg()
        MDFloatingActionButton:
            icon: 'chevron-right'
            pos_hint: {'center_x': .5, 'center_y': .1}
            elevation: 0
            on_release:
                root.load_cur_question(app)

#        MDLabel:
#            text: '00:00'
#            halign: 'center'
#            pos_hint: {'center_x': .5, 'center_y': .1}